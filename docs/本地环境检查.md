# 本地环境检查

## 1. 检查日期

2026-06-13

## 2. 当前结论

用户的开发工具大多已经安装在 `D:\develop` 下，但部分工具没有正确加入环境变量，部分服务尚未启动或需要账号密码。

## 3. 已发现工具

| 工具 | 当前状态 | 路径 / 版本 |
| --- | --- | --- |
| Node.js | 可用 | `v24.14.0` |
| npm | 可用，但 PowerShell 脚本策略会拦截 `npm.ps1` | `D:\develop\node\npm.cmd`，版本 `11.9.0` |
| Python | 可用 | `D:\develop\python\python.exe`，版本 `3.8.10` |
| pip | 可用 | `pip 21.1.1` |
| JDK 17 | 已安装，可用完整路径运行 | `D:\develop\jdk17`，版本 `17.0.15` |
| Maven | 已安装，但当前 `JAVA_HOME` 未正确配置 | `D:\develop\apache-maven-3.9.4` |
| MySQL | 已安装且服务运行中 | `D:\develop\mysql-8.0.34-winx64`，服务名 `MySQL` |
| Redis | 已安装，但当前未运行服务 | `D:\develop\Redis-x64-3.2.100` |

## 4. 当前问题

### 4.1 npm

直接执行：

```text
npm -v
```

会被 PowerShell 执行策略拦截：

```text
无法加载文件 D:\develop\node\npm.ps1，因为在此系统上禁止运行脚本。
```

临时解决方式：

```text
D:\develop\node\npm.cmd
```

后续可以选择修改 PowerShell 执行策略，或在命令中统一使用 `npm.cmd`。

### 4.2 Java / Maven

`D:\develop\jdk17\bin\java.exe` 可用，但命令行直接执行 `java` 找不到。

Maven 报错：

```text
The JAVA_HOME environment variable is not defined correctly
```

后续需要配置：

```text
JAVA_HOME=D:\develop\jdk17
Path 增加：D:\develop\jdk17\bin
Path 增加：D:\develop\apache-maven-3.9.4\bin
```

### 4.3 MySQL

MySQL 服务正在运行。

但执行无密码 ping 时返回：

```text
Access denied for user 'ODBC'@'localhost' (using password: NO)
```

说明服务可达，但需要正确用户名和密码。

后续需要确认 MySQL 用户名和密码，常见是：

```text
root / 自己设置的密码
```

当前已确认本地开发 MySQL：

```text
用户名：root
密码：1234
```

已成功执行：

```text
data/sql/001_init_schema.sql
```

已创建数据库：

```text
love_travel
```

已创建表：

```text
ai_polish_log
app_user
couple_space
couple_space_member
invite_code
travel_plan_day
trip
trip_image
trip_post
user_auth_identity
```

注意：该密码仅作为本地开发记录，后续线上环境必须使用独立强密码和环境变量配置。

### 4.4 Redis

Redis 可执行文件存在：

```text
D:\develop\Redis-x64-3.2.100\redis-server.exe
D:\develop\Redis-x64-3.2.100\redis-cli.exe
```

但当前 6379 端口没有 Redis 服务：

```text
Could not connect to Redis at 127.0.0.1:6379
```

后续需要启动 Redis。

当前已验证可用以下方式启动 Redis：

```cmd
scripts\start-redis.cmd
```

启动成功后应返回：

```text
PONG
```

## 5. 建议下一步

优先顺序：

1. 配置当前终端临时 `JAVA_HOME` 和 Maven 路径。
2. 确认 MySQL root 密码。
3. 启动 Redis。
4. 执行 `data/sql/001_init_schema.sql` 建库建表。
5. 安装并运行 Python AI 服务依赖。
6. 安装并运行 Next.js 前端依赖。
7. 启动 Spring Boot 后端。

## 6. 已新增脚本

已新增临时环境配置脚本：

```text
scripts/dev-env.ps1
```

用途：在当前 PowerShell 窗口中临时配置 `JAVA_HOME`、JDK、Maven 和 Node 路径。

使用方式：

```powershell
.\scripts\dev-env.ps1
```

已新增环境检查脚本：

```text
scripts/check-env.ps1
```

使用方式：

```powershell
.\scripts\check-env.ps1
```

注意：如果 PowerShell 执行策略阻止运行 `.ps1`，可以先用单行命令临时配置环境，或后续再调整执行策略。

当前已确认 PowerShell 会阻止 `.ps1` 脚本运行，因此又新增了 `.cmd` 版本：

```text
scripts/dev-env.cmd
scripts/check-env.cmd
```

在命令提示符 `cmd` 中使用：

```cmd
scripts\dev-env.cmd
scripts\check-env.cmd
```

注意：`.cmd` 脚本设置的环境变量只对当前命令提示符窗口有效。

## 7. 骨架运行验证

### 7.1 Python FastAPI AI 服务

已完成：

1. 依赖安装成功。
2. 应用导入成功。
3. Python 文件编译检查通过。
4. `/internal/health` HTTP 健康检查通过。
5. 未配置 `ALIYUN_DASHSCOPE_API_KEY` 时，AI 润色接口能返回明确错误。

### 7.2 Next.js 前端

已完成：

1. 使用项目内 `.npm-cache` 安装依赖成功。
2. `npm run build -- --webpack` 构建通过。
3. `npm run dev -- --hostname 127.0.0.1 --port 3000 --webpack` 可稳定启动。
4. 访问 `http://127.0.0.1:3000` 返回 200。

注意：前端路径已由用户调整为：

```text
apps/front
```

注意：当前环境不要使用 Next.js 16 默认 Turbopack 后台启动方式，容易出现页面一闪后服务退出。

### 7.3 Spring Boot 后端

已完成：

1. Maven clean package 通过。
2. 已补充 Spring Boot 可执行 jar 配置。
3. `java -jar target/love-travel-server-0.1.0.jar` 可启动。
4. 早期测试用 `/api/health` 接口已移除；本地检查改为确认 8080 端口可连接。
